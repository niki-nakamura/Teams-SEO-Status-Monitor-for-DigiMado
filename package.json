{
  "name": "teams-seo-status-monitor-for-digimado",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "crawl": "node scripts/crawl_links.js",
    "check": "node scripts/check_404.js",
    "monitor": "node scripts/monitor_teams.js",
    "test": "npm run crawl && npm run check && npm run monitor"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.10",
    "...": "..." 
  },
  "devDependencies": {
    // （必要に応じてテストフレームワークなどを記載します）
  }
}
